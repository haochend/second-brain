# Memory System Configuration
MEMORY_HOME=~/.memory
OLLAMA_MODEL=gpt-oss:120b      # Or llama3.2 for faster processing
WHISPER_MODEL=large             # Or base for faster transcription
DEBUG=false

# Optional: Keep audio files after processing (default is to delete them)
# KEEP_AUDIO_AFTER_PROCESSING=true  # Set to true if you want to keep audio files